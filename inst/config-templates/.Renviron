# secret for decoding JSON web token
JWT_SECRET=secret

# path to variables to send emails using aws.ses
EMAIL_VARS=path/to/email/vars.R
SEND_EMAIL=TRUE

# credentials for Amazon SES
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# MongoDB database to store users collection
USERS_DB=test
